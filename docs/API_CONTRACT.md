# API Contract

Contract v1 is frozen: see `contracts/v1/README.md` (request/response/error shapes, generated JSON schema, real example response).

Changes are additive only; a removed or renamed field is v2.
